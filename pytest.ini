[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short --strict-markers
markers =
    behavior: deterministic behavior with external services replaced
    integration: real disposable PostgreSQL or connected component lifecycle
