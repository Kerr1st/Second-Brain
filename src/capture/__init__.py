"""Capture component — self-contained ingestion sources.

Each source produces metadata-headed markdown and feeds the shared
ingestion pipeline (``src.ingest.ingest_content``). Phase 0: ``youtube``.
"""
